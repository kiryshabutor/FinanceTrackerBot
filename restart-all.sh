#!/bin/bash

# Скрипт для автоматической перезагрузки всех контейнеров и запуска cloudflared туннеля

set -e  # Остановка при ошибке

echo "🛑 Остановка всех запущенных контейнеров..."
docker-compose down || true  # Игнорируем ошибку, если контейнеры не запущены

echo ""
echo "🔨 Пересборка и запуск всех контейнеров..."
docker-compose up -d --build

echo ""
echo "⏳ Ожидание готовности сервисов..."
sleep 5

echo ""
echo "✅ Контейнеры успешно запущены!"
echo ""
echo "🚀 Запуск cloudflared туннеля..."
echo ""

# Запускаем cloudflared через существующий скрипт
bash ./start-cloudflared.sh

